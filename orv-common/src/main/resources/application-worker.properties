# Worker Configuration for Video Duration Calculation

# Disable web server for worker (no need for Tomcat)
spring.main.web-application-type=none

# Enable duration calculation worker
worker.duration-calculation.enabled=true

# Number of worker threads (adjust based on available memory and CPU)
worker.duration-calculation.threads=2

# Poll interval in milliseconds when no jobs are available
worker.duration-calculation.poll-interval-ms=3000

# Stuck job threshold in minutes - jobs in PROCESSING state longer than this will be retried
worker.duration-calculation.stuck-threshold-minutes=10